<script type="text/ng-template" id="promethee/move/components">
  <div ng-init="type = component.type; components = component.children">
    <div
      ng-repeat="component in components"
      class="promethee-editor__move--draggable promethee-editor__move--draggable--{{type}}"
      draggable
      data-index="{{$index}}"
      data-type="{{component.type}}"
    >
      <ng-include src="'promethee/move/component'"></ng-include>
      <div
        class="promethee-editor__move--droppable promethee-editor__move--droppable--{{type}}"
        droppable
        data-index="{{$index+1}}"
        data-type="{{type}}"
      >
      </div>
    </div>
  </div>
</script>
